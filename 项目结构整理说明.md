# MIKUEngine 项目结构与构建说明（对齐 Horizon）

本文档记录参考 `HorizonDev-refactor` 完成的整体重构：目录结构、三段式工程、premake 组织、
第三方库/子模块处理、以及**每个 premake 文件的作用**。

---

## 一、整体项目架构

```
MIKUEngine/                         # 仓库根
├── premake5.lua                    # 根构建脚本（workspace 级）
├── GenerateProjects.bat            # 调 ThirdParty/premake 生成 VS 工程
├── MIKUEngine.sln                  # premake 生成的解决方案
├── README.md                       # 拉取 / 编译 / 运行说明
├── 项目结构整理说明.md              # 本文档
│
├── Build/                          # premake 生成（工程文件 + 产物）
│   ├── <Project>/*.vcxproj         # 各工程文件（location 指向此处）
│   ├── Bin/<Config>/               # 最终产物：.lib / .exe
│   └── Obj/<Config>/<Project>/     # 中间产物
│
├── Source/                         # 所有一方工程
│   ├── MikuEngine/                 # 引擎（StaticLib）
│   │   ├── premake5.lua
│   │   └── src/
│   │       ├── mikupch.{h,cpp}     # 预编译头
│   │       ├── Miku.h              # 引擎聚合头
│   │       └── Miku/
│   │           ├── Foundation/     # 基础库（数学/内存/杂项/基本类型）
│   │           ├── Engine/         # 引擎核心：Core/Events/Application/Layer/
│   │           │                   #   LayerStack/Log/Window/GlfwWindow/RenderContext/EntryPoint
│   │           ├── Input/          # 输入：Input/KeyCodes/MouseButtonCodes
│   │           ├── Rendering/      # 渲染：RenderBackend(RHI) + RenderGraph
│   │           ├── ImGui/          # 旧 ImGuiLayer（已从工程排除，保留备查）
│   │           └── (Platform/)     # 旧临时平台代码（已从工程排除，保留备查）
│   │
│   ├── MikuEditor/                 # 编辑器（StaticLib 壳）
│   │   ├── premake5.lua
│   │   └── Source/
│   │       ├── Editor/MikuEditorModule.h     # 对外模块头
│   │       └── Framework/MikuEditor.{h,cpp}  # MikuEditorMain + MikuEditorApp
│   │
│   └── MikuEditorLauncher/         # 可执行入口（ConsoleApp）
│       ├── premake5.lua
│       └── MikuEditorLauncherWin32.cpp        # main() → MikuEditorMain()
│
└── ThirdParty/                     # 第三方库集中目录
    ├── imgui/   (submodule)        # 独立 StaticLib 工程（核心 + dx12/glfw backend）
    │   └── premake5.lua
    ├── glm/     (submodule)        # 纯头文件，仅作 includedir
    ├── spdlog/  (submodule)        # 纯头文件，仅作 includedir
    ├── DX12/    (submodule)        # DirectX-Headers
    ├── glfw/glfw-3.4.bin.WIN64/    # GLFW 预编译库（随仓库提交，非 submodule）
    └── premake/                    # premake5.exe 工具
```

**解决方案分组 / 依赖链**
```
Engine/      MikuEngine          (StaticLib)  ← links imgui + glfw3.lib + d3d12/dxgi/d3dcompiler
Editor/      MikuEditor          (StaticLib)  ← links MikuEngine + imgui
App/         MikuEditorLauncher  (ConsoleApp) ← links MikuEngine + MikuEditor + imgui + glfw3.lib + d3d12/...
ThirdParty/  imgui               (StaticLib)  ← links glfw3.lib
```
启动链路：`Launcher.main() → MikuEditorMain()（Editor 库）→ MikuEditorApp : Application → Run()`。

---

## 二、每个 premake 文件的作用

### 1. 根 `premake5.lua`（workspace 级）
- 定义 **路径辅助函数**：`sourcepath()`、`thirdpartypath()`、`sourcedirs()`（对齐 Horizon）。
- 定义输出变量 `outputdir`，第三方 include 目录表 `IncludeDir`，预编译库表 `Lib`（如 `Lib.GLFW`）。
- 定义 `GLFWDir` 指向预编译 GLFW 根目录。
- 声明 `workspace "MIKUEngine"`：架构 x64、启动项目 `MikuEditorLauncher`、配置 `Debug/Release/Dist`、
  `staticruntime "Off"`（动态 CRT，匹配 GLFW 预编译库）、各配置的 `filter`。
- 用 `group "..." / include "路径"` 引入各子工程（**不** include imgui/glfw 作为源码，imgui 单独成工程）。

### 2. `Source/MikuEngine/premake5.lua`（引擎）
- `kind "StaticLib"`，C++17，PCH = `mikupch.h`。
- `location/targetdir/objdir` 指向 `Build/`。
- `sourcedirs { "src" }` 收集引擎源码；`removefiles` 排除旧的 `src/Platform/**` 与 `src/Miku/ImGui/**`
  （临时代码，保留磁盘、从工程剔除）。
- `includedirs`：`src` + spdlog/imgui/GLFW/DX12/glm。
- `links`：`imgui`（独立工程）、`%{Lib.GLFW}`（预编译）、`d3dcompiler/D3D12/dxgi`。

### 3. `Source/MikuEditor/premake5.lua`（编辑器）
- `kind "StaticLib"`；`sourcedirs { "Source" }`。
- `includedirs`：自身 `Source` + 引擎 `src` + 第三方头。
- `links`：`MikuEngine`、`imgui`。
- 内容为最简空壳（`MikuEditorModule.h` + `MikuEditor.{h,cpp}`，提供 `MikuEditorMain`）。

### 4. `Source/MikuEditorLauncher/premake5.lua`（可执行入口）
- `kind "ConsoleApp"`；`files { "**.h","**.cpp" }`。
- `includedirs`：编辑器 `Source` + 引擎 `src` + 第三方头。
- `links`：`MikuEngine`、`MikuEditor`、`imgui`、`%{Lib.GLFW}`、`d3dcompiler/D3D12/dxgi`
  （最终 exe 需显式链接所有外部 .lib，静态库的原始 .lib 依赖不会自动传递到 exe）。

### 5. `ThirdParty/imgui/premake5.lua`（imgui 工程）
- `kind "StaticLib"`；编译 imgui 核心 + `backends/imgui_impl_dx12.cpp` + `backends/imgui_impl_glfw.cpp`。
- `includedirs`：imgui 根 + GLFW + DX12；`links`：`%{Lib.GLFW}`。
- 对齐 Horizon：imgui 作为独立工程并顺带编译其 backend。

### 6. `GenerateProjects.bat`
- 调 `ThirdParty\premake\premake5.exe vs2022` 生成工程与解决方案。

---

## 三、premake 关键语法/概念

| 概念 | 说明 |
| --- | --- |
| `%{wks.location}` / `%{prj.name}` / `%{cfg.buildcfg}` | 生成期 Token，写入 `.vcxproj` 时展开 |
| `sourcepath()` / `thirdpartypath()` / `sourcedirs()` | Lua 辅助函数（premake 脚本即 Lua），封装路径与批量 `files` |
| `group "X" / include "path" / group ""` | `group` 仅影响 sln 虚拟分组；`include` 执行目标目录的 premake5.lua（共享同一 Lua 环境）|
| `kind` | 工程产物类型（StaticLib / ConsoleApp）|
| `files{}` / `removefiles{}` | 加入 / 从工程剔除源文件（`removefiles` 不删磁盘）|
| `links{}` | 值为**工程名**（建立 ProjectReference）或**具体 .lib 路径**（仅链接）|
| `staticruntime "Off"` | 使用动态 CRT(`/MD`)，需与预编译库运行时一致 |
| `filter "..."` | 条件作用域，到下一个 `filter` 或 `filter {}` 复位 |

---

## 四、第三方库 / 子模块处理

- `imgui / glm / spdlog / DX12`：git **submodule**，已从旧路径 `MIKUEngine/vender/*` 迁移到 `ThirdParty/*`
  （提交记录为重命名，SHA 不变）。克隆需 `--recurse-submodules`。
- `GLFW`：**移除 submodule**，改用仓库内提交的预编译库 `ThirdParty/glfw/glfw-3.4.bin.WIN64`
  （选用 `lib-vc2022/glfw3.lib`，为 `/MD` 版本，匹配 `staticruntime "Off"`）。
- `glm / spdlog / DX12`：纯头文件/头库，仅作 `includedir`，不建工程。

---

## 五、渲染后端（RHI）现状

- 当前可执行是**可运行空壳**：GLFW 窗口 + 可退出主循环。
- 旧 `Platform/`（GLFW 窗口 + DX12Temp）与旧 `Miku/ImGui`（ImGuiLayer）已从工程排除，改由引擎内
  `Engine/GlfwWindow`（窗口）+ `Engine/RenderContext`（RHI 胶水）替代。
- DX12 RHI（`Rendering/RenderBackend`）仍为 WIP，接入代码在 `RenderContext.cpp`，由编译开关
  `MIKU_ENABLE_RENDER_BACKEND`（默认 `0`）控制；完善后置 `1` 启用真正的清屏/present。

---

## 六、构建注意事项

- 修改源码文件增删或任意 `premake5.lua` 后，重跑 `GenerateProjects.bat`。
- 命令行构建若遇 `STL1001`（MSVC 编译器与 STL 头版本不一致，本机装了 14.38 与 14.44 两套工具集），
  可在 VS IDE 内构建，或命令行加 `/p:VCToolsVersion=14.38.33130` 统一工具集。
