# DX12 RenderBackend 移植差异分析

> 对比对象
> - **当前项目（MIKU）**：`d:\MIKUEngine\MIKUEngine\src\Miku\RenderBackend\Direct3D12RenderBackend\`
> - **参考项目（Horizon）**：`C:\Users\qilehe\Downloads\HorizonDev-refactor\Source\HorizonEngine\Source\Rendering\RenderBackend\Direct3D12RenderBackend\`
>
> 分析时间：2026-07-10

---

## 一、结论速览

两个项目的 DX12 后端架构完全一致（抽象基类 `RenderBackend` + `class D3D12RenderBackend : public RenderBackend` 继承 override 实现），当前项目基本是从参考项目移植而来。核心资源与绘制链路（Buffer / Texture / Sampler / Shader / SwapChain / Bindless 描述符 / Draw / Dispatch / RenderPass / Barrier / TimingQuery）均已移植到位。

**尚未移植的部分主要集中在「硬件光线追踪（Hardware Ray Tracing）」整套 API，在接口层、实现层、命令层被成套注释禁用。**

> 注意：`Destroy*` / `UploadTexture` / `GetTextureReadbackData` 在**参考项目本身也是空实现**，属于双方一致，不列为移植缺口（见第四节）。

---

## 二、未移植的 API 清单（光线追踪整套）

### 2.1 接口层 `RenderBackendInterface.h`（当前项目已注释）

| API | 当前项目状态 | 参考项目状态 |
|-----|------------|------------|
| `GetAccelerationStructureSRVBindlessResourceDescriptorIndex` | 注释 (`//`)，行 180 | 已启用（纯虚） |
| `CreateRayTracingBottomLevelAccelerationStructure` | 注释，行 235 | 已启用（纯虚） |
| `CreateRayTracingTopLevelAccelerationStructure` | 注释，行 240 | 已启用（纯虚） |
| `CreateRayTracingPipelineState` | 注释，行 245 | 已启用（纯虚） |
| `CreateRayTracingShaderBindingTable` | 注释，行 250 | 已启用（纯虚） |

### 2.2 实现层 `Direct3D12RenderBackendPrivate.h`（当前项目已注释）

`class D3D12RenderBackend` 中以下 `override` 声明被整体注释（行 2487、2497-2500）：

- `GetAccelerationStructureSRVBindlessResourceDescriptorIndex` override
- `CreateRayTracingBottomLevelAccelerationStructure` override
- `CreateRayTracingTopLevelAccelerationStructure` override
- `CreateRayTracingPipelineState` override
- `CreateRayTracingShaderBindingTable` override

`D3D12Device` 内部实现函数被注释（行 2089、2442-2443）：

- `CreateD3D12RayTracingBottomLevelAccelerationStructure`
- `CreateD3D12RayTracingPipelineState`
- `GetRayTracingPipelineStateObject`

> 备注：光追相关的**数据结构**在当前项目里其实已经保留（未注释），例如 `struct D3D12RayTracingAccelerationStructure`（行 335）、`struct D3D12RayTracingPipelineStateObject`（行 414）、`accelerationStructures` / `rayTracingPipelineStateObjects` 容器、`AllocateRayTracingAccelerationStructure()`（行 1953）等。缺的是把这些结构串起来的 **API 实现函数**。

### 2.3 命令层 `Direct3D12RenderBackend.cpp` → `CompileRenderBackendCommands`（当前项目已注释）

**同步命令队列**（当前项目行 87-89 注释）：

| 命令 | 当前项目 | 参考项目 |
|------|---------|---------|
| `RenderBackendCommandBuildRayTracingBottomLevelAccelerationStructure` | 注释 | 已注册，实现体在 cpp:616 |
| `RenderBackendCommandBuildRayTracingTopLevelAccelerationStructure` | 注释 | 已注册，实现体在 cpp:648 |
| `RenderBackendCommandDispatchRays` | 注释 | 已注册，实现体在 cpp:680 |

**异步命令队列**（当前项目行 115-116 注释）：

| 命令 | 当前项目 | 参考项目 |
|------|---------|---------|
| `RenderBackendCommandBuildRayTracingBottomLevelAccelerationStructure` | 注释 | 已注册 |
| `RenderBackendCommandBuildRayTracingTopLevelAccelerationStructure` | 注释 | 已注册 |

对应的命令实现函数 `CompileRenderBackendCommand(...)` 在当前项目 `Private.h` 中也被注释（行 2584-2586）：

- `CompileRenderBackendCommand(const RenderBackendCommandBuildRayTracingBottomLevelAccelerationStructure&)`
- `CompileRenderBackendCommand(const RenderBackendCommandBuildRayTracingTopLevelAccelerationStructure&)`
- `CompileRenderBackendCommand(const RenderBackendCommandDispatchRays&)`

---

## 三、其它移植差异

| 项目 | 当前项目 | 参考项目 | 说明 |
|------|---------|---------|------|
| `Direct3D12WorkGraph.cpp` | **不存在** | 存在，但**为 0 字节空文件** | 参考项目仅占位，无实际逻辑，可忽略 |
| `dispatchRaysIndirectCommandSignature` | 结构成员保留（Private.h:599） | 存在 | 光追间接派发的命令签名，随光追一并未启用 |
| `DispatchIndirect` 光追分支 | — | cpp 内 `// TODO: if (supportDispatchRaysIndirect)` | 参考项目本身也是 TODO 占位 |

已成功移植且两边一致的命令（无差异）：
`CopyBuffer`、`CopyTexture`、`UpdateBuffer`、`UpdateTexture`、`ClearBufferUAV`、`ClearTextureUAV`、`Barriers`、`BeginTimingQuery`、`EndTimingQuery`、`ResolveTimingQueryResults`、`Dispatch`、`DispatchIndirect`、`SetViewport`、`SetScissor`、`SetStencilReference`、`BeginRenderPass`、`EndRenderPass`、`Draw`、`DrawIndirect`、`DispatchMesh`、`DispatchMeshIndirect`、`BeginDebugLabel`、`EndDebugLabel`、`DispatchSuperSampling`。

---

## 四、双方一致的「空实现」（非移植缺口，仅提示）

以下 API 在**当前项目与参考项目中均为空函数体**，属于原项目就未落地的部分，不算“当前项目漏移植”。但如果后续要让资源正确释放/上传，两边都需要补实现：

| API | 当前项目 | 参考项目 | 风险 |
|-----|---------|---------|------|
| `DestroySwapChain` | 空 (cpp:1469) | 空 (cpp:1469) | 资源未释放 |
| `DestroyBuffer` | 空 (cpp:1604) | 空 | 资源泄漏 |
| `DestroyTexture` | 空 (cpp:1618) | 空 | 资源泄漏 |
| `DestroySampler` | 空 | 空 | — |
| `DestroyShader` | 空 | 空 | — |
| `DestroyTimingQueryHeap` | 空 | 空 | — |
| `UploadTexture` | 空 (cpp:1623) | 空 | 纹理上传无效果 |
| `GetTextureReadbackData` | 空 (cpp:1628) | 空 | 回读无效果 |

---

## 五、补齐移植的建议顺序

若要把光线追踪能力移植齐全，建议按依赖顺序恢复：

1. **接口层**：取消 `RenderBackendInterface.h` 中 5 个 RayTracing 相关纯虚函数的注释（行 180/235/240/245/250）。
2. **实现声明**：取消 `Direct3D12RenderBackendPrivate.h` 中对应 `override` 声明（行 2487、2497-2500）与 `D3D12Device` 内部实现声明（行 2089、2442-2443、2584-2586）的注释。
3. **实现体**：从参考项目 `Direct3D12RenderBackend.cpp` 移植以下函数体：
   - `CreateRayTracingBottomLevelAccelerationStructure` / `TopLevel...`
   - `CreateRayTracingPipelineState`
   - `CreateRayTracingShaderBindingTable`
   - `GetAccelerationStructureSRVBindlessResourceDescriptorIndex`
   - 命令实现：`CompileRenderBackendCommand` 的 BLAS/TLAS Build（cpp:616/648）与 `DispatchRays`（cpp:680）
4. **命令注册**：在当前项目 `CompileRenderBackendCommands`（同步 87-89 行、异步 115-116 行）取消对应 `COMPILE_RENDER_COMMAND` 注释。
5. **编译验证**：确认 `enableHardwareRayTracing`（Private.h:2525）开关链路，以及 `dispatchRaysIndirectCommandSignature` 的创建位置随之启用。

---

## 附：整体 API 覆盖对照总表

| 分类 | API | 当前项目 | 参考项目 |
|------|-----|:-------:|:-------:|
| 设备 | `Tick` / `CreateRenderDevices` / `DestroyRenderDevices` / `FlushRenderDevices` / `GetNativeDevice` | ✅ | ✅ |
| 交换链 | `CreateSwapChain` / `ResizeSwapChain` / `PresentSwapChain` / `GetActiveSwapChainBuffer` | ✅ | ✅ |
| 交换链 | `DestroySwapChain` | ⚠️空 | ⚠️空 |
| Buffer | `CreateBuffer` / `ResizeBuffer` / `MapBuffer` / `UnmapBuffer` / `UpdateBuffer` | ✅ | ✅ |
| Buffer | `DestroyBuffer` | ⚠️空 | ⚠️空 |
| Texture | `CreateTexture` / `CreateTextureView` | ✅ | ✅ |
| Texture | `DestroyTexture` / `UploadTexture` / `GetTextureReadbackData` | ⚠️空 | ⚠️空 |
| Bindless | Texture SRV/UAV、Buffer CBV/SRV/UAV 描述符索引 | ✅ | ✅ |
| Bindless | `GetAccelerationStructureSRVBindlessResourceDescriptorIndex` | ❌注释 | ✅ |
| Sampler | `CreateSampler` | ✅ | ✅ |
| Sampler | `DestroySampler` | ⚠️空 | ⚠️空 |
| Shader | `CreateShader` | ✅ | ✅ |
| Shader | `DestroyShader` | ⚠️空 | ⚠️空 |
| Query | `CreateTimingQueryHeap` / `SubmitCommandLists` | ✅ | ✅ |
| Query | `DestroyTimingQueryHeap` | ⚠️空 | ⚠️空 |
| 调试 | `SetObjectName` (Texture/Buffer) | ✅ | ✅ |
| **光追** | `CreateRayTracingBottomLevelAccelerationStructure` | ❌注释 | ✅ |
| **光追** | `CreateRayTracingTopLevelAccelerationStructure` | ❌注释 | ✅ |
| **光追** | `CreateRayTracingPipelineState` | ❌注释 | ✅ |
| **光追** | `CreateRayTracingShaderBindingTable` | ❌注释 | ✅ |
| **光追命令** | `BuildRayTracingBLAS` / `BuildRayTracingTLAS` / `DispatchRays` | ❌注释 | ✅ |

图例：✅ 已实现并启用 ｜ ⚠️空 双方均空实现（占位） ｜ ❌注释 当前项目注释禁用、参考项目已启用（**待移植**）
